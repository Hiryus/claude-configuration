* All repository instructions are in the file `AGENTS.md`. Open this file instead of `claude.md`.
* In all interactions and commit messages, be extremely concise and sacrifice grammar for the sake of concision.
* When using shell commands, prefer simple commands over complex ones and several tools calls over long pipe chains (ex: avoid "cmd a & cmd b & cmd c" or "cmd a | cmd b | cmd c").
* If you need to run any bash command, clearly state what they do and how they work, then why you need to use them. Else the user will not validate them.
* Always quote paths in your bash commands.
* When writing markdown, format the tables correctly so that each column border lines up.